{/*
  Sovereign Academy — บทเรียนแบบโต้ตอบ
  หัวข้อ : แนะนำอัตราส่วน — การเปรียบเทียบปริมาณ สามรูปแบบ ส่วนต่อรวม
  ระดับ  : ป.6
  Node  : T11_6_0
  Lang  : Thai
*/}

import { useSignal } from "@preact/signals";

{/* ── COMPONENT 1: RatioBuilder — กดเพิ่ม/ลดจำนวน ดูอัตราส่วนแบบ real-time ── */}
export function RatioBuilder() {
  const red = useSignal(3);
  const blue = useSignal(5);
  const MAX = 12;
  function inc(sig) { if (sig.value < MAX) sig.value++; }
  function dec(sig) { if (sig.value > 0) sig.value--; }
  const r = red.value;
  const b = blue.value;

  return (
    <div style={{ background: "#2b2d31", borderRadius: "10px", padding: "20px 24px", margin: "16px 0", border: "1px solid #3f4147" }}>
      <div style={{ fontSize: "12px", color: "#949ba4", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: "12px" }}>สร้างอัตราส่วนด้วยตัวเอง</div>

      {/* แถวสีแดง */}
      <div style={{ display: "flex", alignItems: "center", gap: "10px", marginBottom: "8px" }}>
        <button onClick={() => dec(red)} style={{ width: "28px", height: "28px", background: "#383a40", border: "1px solid #4e5058", borderRadius: "4px", color: "#f2f3f5", fontSize: "16px", cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>−</button>
        <span style={{ color: "#f23f43", fontWeight: "700", fontSize: "16px", minWidth: "20px", textAlign: "center" }}>{r}</span>
        <button onClick={() => inc(red)} style={{ width: "28px", height: "28px", background: "#383a40", border: "1px solid #4e5058", borderRadius: "4px", color: "#f2f3f5", fontSize: "16px", cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>+</button>
        <div style={{ display: "flex", gap: "4px", flexWrap: "wrap" }}>
          {Array.from({ length: r }, (_, i) => (
            <span key={i} style={{ fontSize: "20px" }}>🔴</span>
          ))}
        </div>
      </div>

      {/* แถวสีน้ำเงิน */}
      <div style={{ display: "flex", alignItems: "center", gap: "10px", marginBottom: "16px" }}>
        <button onClick={() => dec(blue)} style={{ width: "28px", height: "28px", background: "#383a40", border: "1px solid #4e5058", borderRadius: "4px", color: "#f2f3f5", fontSize: "16px", cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>−</button>
        <span style={{ color: "#5865f2", fontWeight: "700", fontSize: "16px", minWidth: "20px", textAlign: "center" }}>{b}</span>
        <button onClick={() => inc(blue)} style={{ width: "28px", height: "28px", background: "#383a40", border: "1px solid #4e5058", borderRadius: "4px", color: "#f2f3f5", fontSize: "16px", cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center" }}>+</button>
        <div style={{ display: "flex", gap: "4px", flexWrap: "wrap" }}>
          {Array.from({ length: b }, (_, i) => (
            <span key={i} style={{ fontSize: "20px" }}>🔵</span>
          ))}
        </div>
      </div>

      {/* แสดงอัตราส่วน */}
      {r === 0 && b === 0
        ? <div style={{ color: "#949ba4", fontSize: "13px" }}>เพิ่มของเข้ามาเพื่อสร้างอัตราส่วน!</div>
        : <div style={{ display: "flex", gap: "16px", flexWrap: "wrap" }}>
            <div style={{ background: "#313338", borderRadius: "6px", padding: "8px 14px", borderLeft: "4px solid #f0b232" }}>
              <div style={{ color: "#949ba4", fontSize: "10px", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: "2px" }}>คำ</div>
              <div style={{ color: "#f2f3f5", fontSize: "16px", fontWeight: "600" }}>{r} ต่อ {b}</div>
            </div>
            <div style={{ background: "#313338", borderRadius: "6px", padding: "8px 14px", borderLeft: "4px solid #5865f2" }}>
              <div style={{ color: "#949ba4", fontSize: "10px", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: "2px" }}>โคลอน</div>
              <div style={{ color: "#f2f3f5", fontSize: "16px", fontWeight: "600" }}>{r} : {b}</div>
            </div>
            <div style={{ background: "#313338", borderRadius: "6px", padding: "8px 14px", borderLeft: "4px solid #23a55a" }}>
              <div style={{ color: "#949ba4", fontSize: "10px", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: "2px" }}>เศษส่วน</div>
              <div style={{ color: "#f2f3f5", fontSize: "16px", fontWeight: "600" }}>{r}/{b}</div>
            </div>
          </div>
      }

      {/* แผนภาพแท่ง */}
      {(r > 0 || b > 0) && (
        <div style={{ marginTop: "14px" }}>
          <svg viewBox={`0 0 320 36`} style={{ width: "100%", maxWidth: "400px", display: "block" }}>
            {(() => {
              const total = r + b || 1;
              const rw = (r / total) * 300;
              const bw = (b / total) * 300;
              return (
                <g>
                  <rect x="10" y="4" width={rw} height="28" rx="4" fill="#f23f43" opacity="0.8" />
                  {rw > 30 && <text x={10 + rw / 2} y="22" text-anchor="middle" fill="#f2f3f5" font-size="12" font-family="sans-serif" font-weight="700">{r}</text>}
                  <rect x={10 + rw} y="4" width={bw} height="28" rx="4" fill="#5865f2" opacity="0.8" />
                  {bw > 30 && <text x={10 + rw + bw / 2} y="22" text-anchor="middle" fill="#f2f3f5" font-size="12" font-family="sans-serif" font-weight="700">{b}</text>}
                </g>
              );
            })()}
          </svg>
        </div>
      )}
    </div>
  );
}

{/* ── COMPONENT 2: OrderMatters — สลับด้านเพื่อเรียนรู้ว่าลำดับสำคัญ ── */}
export function OrderMatters() {
  const swapped = useSignal(false);
  const a = 4, b = 7;
  const first = swapped.value ? b : a;
  const second = swapped.value ? a : b;
  const label1 = swapped.value ? "เด็กผู้หญิง" : "เด็กผู้ชาย";
  const label2 = swapped.value ? "เด็กผู้ชาย" : "เด็กผู้หญิง";
  return (
    <div style={{ background: "#2b2d31", borderRadius: "10px", padding: "16px 20px", margin: "16px 0", border: "1px solid #3f4147", textAlign: "center" }}>
      <div style={{ fontSize: "12px", color: "#949ba4", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: "10px" }}>⚡ ลำดับสำคัญไหม?</div>
      <div style={{ marginBottom: "14px" }}>
        <span style={{ fontSize: "13px", color: "#b5bac1" }}>ในห้องเรียน: </span>
        <span style={{ color: "#5865f2", fontWeight: "700" }}>เด็กผู้ชาย 4 คน</span>
        <span style={{ fontSize: "13px", color: "#b5bac1" }}> และ </span>
        <span style={{ color: "#f23f43", fontWeight: "700" }}>เด็กผู้หญิง 7 คน</span>
      </div>
      <div style={{ fontSize: "22px", fontWeight: "700", color: "#f2f3f5", marginBottom: "8px" }}>
        <span style={{ color: swapped.value ? "#f23f43" : "#5865f2" }}>{label1}</span>
        <span style={{ color: "#949ba4" }}> : </span>
        <span style={{ color: swapped.value ? "#5865f2" : "#f23f43" }}>{label2}</span>
        <span style={{ color: "#949ba4" }}> = </span>
        <span style={{ color: "#f0b232" }}>{first} : {second}</span>
      </div>
      <button onClick={() => (swapped.value = !swapped.value)} style={{ background: "#5865f2", border: "none", borderRadius: "6px", padding: "8px 20px", color: "#fff", fontSize: "13px", fontWeight: "600", cursor: "pointer" }}>
        🔄 สลับด้าน
      </button>
      <div style={{ marginTop: "10px", fontSize: "12px", color: swapped.value ? "#f0b232" : "#949ba4", transition: "color 0.2s" }}>
        {swapped.value
          ? "⚠️ สลับลำดับ → ความหมายเปลี่ยน! ตอนนี้เป็นเด็กผู้หญิงต่อเด็กผู้ชาย"
          : "คลิกสลับเพื่อดูว่าลำดับสำคัญอย่างไรในอัตราส่วน"}
      </div>
    </div>
  );
}

{/* ── COMPONENT 3: PartWholeExplorer — สลับระหว่างส่วนต่อส่วน กับ ส่วนต่อรวม ── */}
export function PartWholeExplorer() {
  const mode = useSignal("part");
  const blueCount = 2;
  const yellowCount = 3;
  const total = blueCount + yellowCount;
  const isPart = mode.value === "part";
  const leftVal = blueCount;
  const rightVal = isPart ? yellowCount : total;
  const rightLabel = isPart ? "สีเหลือง" : "ทั้งหมด";
  const rightColor = isPart ? "#f0b232" : "#b5bac1";
  return (
    <div style={{ background: "#2b2d31", borderRadius: "10px", padding: "16px 20px", margin: "16px 0", border: "1px solid #3f4147" }}>
      <div style={{ fontSize: "12px", color: "#949ba4", textTransform: "uppercase", letterSpacing: "0.06em", marginBottom: "10px" }}>ส่วนต่อส่วน กับ ส่วนต่อส่วนรวม</div>
      <div style={{ marginBottom: "12px", fontSize: "14px", color: "#b5bac1" }}>
        ถุงหนึ่งมีลูกหิน<strong style={{ color: "#5865f2" }}>สีน้ำเงิน 2 ลูก</strong> และ<strong style={{ color: "#f0b232" }}>สีเหลือง 3 ลูก</strong> (รวม {total} ลูก)
      </div>
      <div style={{ display: "flex", gap: "8px", marginBottom: "14px" }}>
        <button onClick={() => (mode.value = "part")} style={{ flex: 1, background: isPart ? "#3c3f75" : "#383a40", border: `1px solid ${isPart ? "#5865f2" : "#4e5058"}`, borderRadius: "6px", padding: "8px", color: isPart ? "#f2f3f5" : "#b5bac1", fontSize: "13px", fontWeight: "600", cursor: "pointer" }}>ส่วนต่อส่วน</button>
        <button onClick={() => (mode.value = "whole")} style={{ flex: 1, background: !isPart ? "#3c3f75" : "#383a40", border: `1px solid ${!isPart ? "#5865f2" : "#4e5058"}`, borderRadius: "6px", padding: "8px", color: !isPart ? "#f2f3f5" : "#b5bac1", fontSize: "13px", fontWeight: "600", cursor: "pointer" }}>ส่วนต่อรวม</button>
      </div>
      {/* ภาพ */}
      <div style={{ display: "flex", justifyContent: "center", gap: "6px", marginBottom: "10px", fontSize: "20px" }}>
        <span>🔵</span><span>🔵</span>
        <span style={{ color: "#949ba4", fontSize: "16px", alignSelf: "center", margin: "0 4px" }}>vs</span>
        {isPart
          ? <><span>🟡</span><span>🟡</span><span>🟡</span></>
          : <><span>🔵</span><span>🔵</span><span>🟡</span><span>🟡</span><span>🟡</span></>
        }
      </div>
      <div style={{ textAlign: "center", fontSize: "20px", fontWeight: "700", color: "#f2f3f5" }}>
        <span style={{ color: "#5865f2" }}>น้ำเงิน</span>
        <span style={{ color: "#949ba4" }}> : </span>
        <span style={{ color: rightColor }}>{rightLabel}</span>
        <span style={{ color: "#949ba4" }}> = </span>
        <span style={{ color: "#f0b232" }}>{leftVal} : {rightVal}</span>
      </div>
      <div style={{ marginTop: "10px", textAlign: "center", fontSize: "12px", color: "#b5bac1" }}>
        {isPart
          ? "เปรียบเทียบกลุ่มหนึ่งกับอีกกลุ่มหนึ่ง"
          : "เปรียบเทียบกลุ่มหนึ่งกับจำนวนทั้งหมด"}
      </div>
    </div>
  );
}

{/* ── COMPONENT 4: Quiz — 5 ข้อเกี่ยวกับอัตราส่วน ─────────────────── */}
export function Quiz() {
  const qs = [
    { q: "สูตรอาหารใช้แป้ง 2 ถ้วย และน้ำตาล 3 ถ้วย อัตราส่วนของแป้งต่อน้ำตาลคือ?",
      choices: ["3 : 2", "2 : 3", "2 : 5", "5 : 2"], correct: 1,
      hint: "แป้งมาก่อน (2) แล้วน้ำตาล (3) → 2 : 3" },
    { q: "ที่พักพิงสัตว์มีแมว 5 ตัว และสุนัข 8 ตัว อัตราส่วนของสุนัขต่อแมวคือ?",
      choices: ["5 : 8", "8 : 5", "5 : 13", "8 : 13"], correct: 1,
      hint: "สุนัขมาก่อน (8) แล้วแมว (5) → 8 : 5" },
    { q: "ถุงมีเหรียญสีแดง 3 สีน้ำเงิน 4 สีเขียว 5 อัตราส่วนของแดงต่อทั้งหมดคือ?",
      choices: ["3 : 9", "3 : 5", "3 : 12", "12 : 3"], correct: 2,
      hint: "รวม = 3 + 4 + 5 = 12 ดังนั้นแดงต่อทั้งหมด = 3 : 12" },
    { q: "ข้อใดไม่ใช่วิธีเขียนอัตราส่วนที่ถูกต้อง?",
      choices: ["5 ต่อ 3", "5 : 3", "5 × 3", "5/3"], correct: 2,
      hint: "การคูณ (×) ไม่ใช่สัญลักษณ์อัตราส่วน มีแค่ คำ โคลอน และเศษส่วน" },
    { q: "\"4 : 7\" กับ \"7 : 4\" เป็นอัตราส่วนเดียวกัน",
      choices: ["จริง", "ไม่จริง"], correct: 1,
      hint: "ลำดับสำคัญ! 4 : 7 ≠ 7 : 4 เป็นคนละอัตราส่วน" },
  ];
  const answers = useSignal(Array(qs.length).fill(null));
  const done = useSignal(false);
  const score = answers.value.filter((a, i) => a === qs[i].correct).length;
  function pick(qi, ci) { if (done.value) return; const n = [...answers.value]; n[qi] = ci; answers.value = n; }
  const letters = ["ก","ข","ค","ง"];
  return (
    <div style={{ margin: "20px 0" }}>
      {qs.map((q, qi) => {
        const chosen = answers.value[qi];
        const ok = chosen === q.correct;
        return (
          <div key={qi} style={{ background: "#2b2d31", borderRadius: "10px", padding: "16px 20px", marginBottom: "10px", border: done.value ? `1px solid ${ok ? "#23a55a" : "#f23f43"}` : "1px solid #3f4147", transition: "border-color 0.2s" }}>
            <p style={{ margin: "0 0 10px", fontWeight: "600", color: "#f2f3f5", fontSize: "14px" }}>ข้อ {qi + 1}. {q.q}</p>
            <div style={{ display: "flex", flexDirection: "column", gap: "6px" }}>
              {q.choices.map((c, ci) => {
                let bg = "#383a40", border = "#4e5058", color = "#b5bac1";
                if (chosen === ci && !done.value) { bg = "#3c3f75"; border = "#5865f2"; color = "#f2f3f5"; }
                if (done.value && ci === q.correct) { bg = "#1e3a2a"; border = "#23a55a"; color = "#23a55a"; }
                if (done.value && chosen === ci && ci !== q.correct) { bg = "#3a1e1e"; border = "#f23f43"; color = "#f23f43"; }
                return <button key={ci} onClick={() => pick(qi, ci)} style={{ background: bg, border: `1px solid ${border}`, borderRadius: "6px", padding: "8px 12px", color, fontSize: "13px", textAlign: "left", cursor: done.value ? "default" : "pointer", transition: "all 0.15s" }}>{letters[ci]}. {c}</button>;
              })}
            </div>
            {done.value && <p style={{ margin: "8px 0 0", fontSize: "12px", color: ok ? "#23a55a" : "#f23f43" }}>{ok ? "✓ ถูกต้อง! " : "✗ "}<span style={{ color: "#b5bac1" }}>{q.hint}</span></p>}
          </div>
        );
      })}
      {!done.value
        ? <button onClick={() => (done.value = true)} disabled={answers.value.includes(null)} style={{ background: answers.value.includes(null) ? "#3f4147" : "#5865f2", border: "none", borderRadius: "6px", padding: "10px 24px", color: answers.value.includes(null) ? "#949ba4" : "#fff", fontSize: "14px", fontWeight: "600", cursor: answers.value.includes(null) ? "not-allowed" : "pointer", marginTop: "6px" }}>ส่งคำตอบ</button>
        : <div style={{ padding: "12px 16px", borderRadius: "8px", fontSize: "15px", fontWeight: "700", marginTop: "6px", background: score === 5 ? "#1e3a2a" : score >= 3 ? "#2a2d1e" : "#3a1e1e", color: score === 5 ? "#23a55a" : score >= 3 ? "#f0b232" : "#f23f43", border: `1px solid ${score === 5 ? "#23a55a" : score >= 3 ? "#f0b232" : "#f23f43"}` }}>
            {score === 5 ? "🎉 ถูกทั้ง 5/5!" : score >= 3 ? `⭐ ${score}/5 — เกือบแล้ว!` : `📖 ${score}/5 — ลองทบทวนบทเรียนแล้วลองใหม่`}
            <button onClick={() => { answers.value = Array(qs.length).fill(null); done.value = false; }} style={{ marginLeft: "12px", background: "transparent", border: "1px solid currentColor", borderRadius: "4px", padding: "2px 10px", color: "inherit", cursor: "pointer", fontSize: "12px" }}>ลองอีกครั้ง</button>
          </div>}
    </div>
  );
}

---

# 📏 แนะนำอัตราส่วน

**ชั้น ป.6** · อัตราส่วน อัตรา และสัดส่วน · **Node T11\_6\_0**

🇹🇭 **ไทย** | [🇬🇧 English](/lesson/T11_6_0_EN)

---

## สิ่งที่เราจะเรียนรู้

**อัตราส่วน** คือวิธีการเปรียบเทียบปริมาณสองอย่าง หลังจากเรียนบทนี้จบแล้ว คุณจะสามารถ:

- อธิบายความหมายของอัตราส่วนด้วยคำพูดและสัญลักษณ์
- เขียนอัตราส่วนได้**สามแบบ**
- แยกแยะความแตกต่างระหว่าง ส่วนต่อส่วน กับ ส่วนต่อรวม

---

## แนวคิดหลัก

อัตราส่วนเปรียบเทียบ**ปริมาณสองอย่าง**ที่มีความสัมพันธ์กัน

> มีแอปเปิ้ล**สีแดง 3 ลูก** และแอปเปิ้ล**สีเขียว 5 ลูก**ในชาม

**อัตราส่วน**ของแอปเปิ้ลสีแดงต่อสีเขียวคือ:

$$
\text{แดง} : \text{เขียว} = 3 : 5
$$

---

## สามวิธีในการเขียนอัตราส่วน

ทั้งสามแบบนี้มีความหมายเหมือนกันทุกประการ:

| รูปแบบ | ตัวอย่าง |
|--------|---------|
| คำ | $3 \text{ ต่อ } 5$ |
| โคลอน | $3 : 5$ |
| เศษส่วน | $\dfrac{3}{5}$ |

> ⚠️ **ลำดับสำคัญ!** $3 : 5$ **ไม่เหมือนกับ** $5 : 3$
> เขียนปริมาณ**ตามลำดับที่โจทย์ถาม**เสมอ

### ลองดู — การสลับด้านมีผลไหม?

<OrderMatters />

---

## สร้างอัตราส่วนด้วยตัวเอง

กดปุ่มด้านล่างเพื่อเพิ่มหรือลด 🔴 กับ 🔵
ดูว่าอัตราส่วนทั้ง 3 รูปแบบอัปเดตแบบ real-time:

<RatioBuilder />

---

## ตัวอย่างแบบละเอียด

### ตัวอย่างที่ 1 — การเปรียบเทียบง่ายๆ

> ในห้องเรียนมี**เด็กผู้ชาย 4 คน** และ**เด็กผู้หญิง 7 คน** เขียนอัตราส่วนของเด็กผู้ชายต่อเด็กผู้หญิง

**ขั้นตอนที่ 1:** ระบุสิ่งที่กำลังเปรียบเทียบ

$$
\text{เด็กผู้ชาย} : \text{เด็กผู้หญิง}
$$

**ขั้นตอนที่ 2:** เขียนตัวเลขตามลำดับที่ถูกต้อง

$$
4 : 7
$$

**คำตอบ:** อัตราส่วนของเด็กผู้ชายต่อเด็กผู้หญิงคือ $4 : 7$ (หรือ "$4$ ต่อ $7$" หรือ $\tfrac{4}{7}$)

---

### ตัวอย่างที่ 2 — อัตราส่วนจากรูปภาพ

> 🔵 🔵 🔵 🔴 🔴
>
> อัตราส่วนของวงกลมสีน้ำเงินต่อวงกลมสีแดงคือเท่าไร?

**นับ:** สีน้ำเงิน 3 อัน, สีแดง 2 อัน

$$
\text{น้ำเงิน} : \text{แดง} = 3 : 2
$$

---

### ตัวอย่างที่ 3 — ส่วนต่อรวม

> ถุงหนึ่งมี**ลูกหินสีน้ำเงิน 2 ลูก** และ**ลูกหินสีเหลือง 3 ลูก** (รวม 5 ลูก)
> อัตราส่วนของลูกหินสีน้ำเงินต่อลูกหิน**ทั้งหมด**คือเท่าไร?

ในที่นี้เราเปรียบเทียบ**ส่วนย่อย**กับ**ส่วนรวม**:

$$
\text{น้ำเงิน} : \text{ทั้งหมด} = 2 : 5
$$

### ลองเล่น: ส่วนต่อส่วน กับ ส่วนต่อรวม

สลับโหมดด้านล่างเพื่อดูว่าอัตราส่วนเปลี่ยนอย่างไร:

<PartWholeExplorer />

---

## ข้อผิดพลาดที่พบบ่อย

| ❌ ผิด | ✅ ถูก |
|--------|--------|
| เขียนอัตราส่วนสลับหน้าหลัง | ตรงตามลำดับที่โจทย์ระบุเสมอ |
| ทำให้เศษส่วนต่ำสุดทั้งที่ไม่ได้สั่ง | ปล่อย $4 : 6$ ไว้ตามเดิม เว้นแต่จะถูกสั่ง |
| คิดว่าอัตราส่วน $=$ เศษส่วนเสมอ | $2 : 3$ เปรียบเทียบส่วน; $\tfrac{2}{3}$ หมายถึงส่วนหนึ่งของทั้งหมด |

---

## คำศัพท์สำคัญ

| คำ | ความหมาย |
|----|---------|
| **อัตราส่วน (Ratio)** | การเปรียบเทียบปริมาณสองอย่าง |
| **พจน์ (Terms)** | ตัวเลขสองตัวในอัตราส่วน (เช่น ใน $3:5$ พจน์คือ $3$ กับ $5$) |
| **ส่วนต่อส่วน (Part-to-Part)** | เปรียบเทียบกลุ่มหนึ่งกับอีกกลุ่มหนึ่ง |
| **ส่วนต่อรวม (Part-to-Whole)** | เปรียบเทียบกลุ่มหนึ่งกับทั้งหมด |

---

## สูตรสำคัญ

$$
\boxed{a : b = \frac{a}{b} = a \text{ ต่อ } b}
$$

ทั้งสามรูปแบบอธิบายอัตราส่วนเดียวกัน **ลำดับ**ของ $a$ กับ $b$ เป็นตัวกำหนดความหมาย

---

## ✏️ ทดสอบความเข้าใจ

ตอบคำถามทั้ง 5 ข้อ แล้วกด**ส่งคำตอบ**:

<Quiz />

---

## พร้อมหรือยัง?

เมื่อคุณตอบคำถามด้านบนได้แล้ว คลิก**เริ่มฝึกปฏิบัติ**เพื่อลองทำแบบฝึกหัดให้คะแนนของโหนดนี้

---

*บทเรียนถัดไป → [T11\_6\_1 — แสดงภาพอัตราส่วน](/lesson/T11_6_1_TH)*
